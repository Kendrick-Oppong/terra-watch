# @terra-watch/shared

Shared TypeScript types and constants used across TerraWatch applications.

This package provides a single source of truth for domain types such as mining sites, imagery scenes, risk levels, and report requests.
